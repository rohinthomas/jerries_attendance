import 'package:flutter/material.dart';

void main(){
 runApp(const Jerries());
}
class Jerries extends StatelessWidget {
  const Jerries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
